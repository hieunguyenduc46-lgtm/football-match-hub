# Dev: để TRỐNG (frontend sẽ gọi /api qua Vite proxy sang backend localhost:8000).
# Prod: trỏ tới backend đã deploy, ví dụ:
#   VITE_API_BASE=https://football-match-hub-api.onrender.com/api
VITE_API_BASE=
